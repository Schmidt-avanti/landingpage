source: /
output: dist

collections_config:
  pages:
    path: /
    output: true

editor:
  default_path: index.html
  site_files:
    editable:
      - "*.html"

paths:
  uploads: uploads
  static: ''

timezone: Europe/Belgrade
